import { Arguments } from 'yargs';
import { GlobalArgv } from '../types';
import fs from 'fs';
export declare const DownloadCommand: {
    command: string;
    describe: string;
    builder: (argv: any) => any;
    handler: (argv: Arguments<GlobalArgv>) => Promise<void>;
};
export declare function downloadEnvFiles({ meta, globalEnvFilePath, writeFile, }: {
    writeFile: typeof fs.writeFileSync;
    meta: any;
    globalEnvFilePath?: string;
}): Promise<void>;
