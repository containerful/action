import { Arguments } from 'yargs';
import { GlobalArgv } from '../types';
export declare const UploadCommand: {
    command: string;
    describe: string;
    builder: (argv: any) => any;
    handler: (argv: Arguments<GlobalArgv>) => Promise<void>;
};
export declare function uploadEnvFiles({ dockerComposeObject, meta, globalEnvFilePath, }: {
    dockerComposeObject: any;
    meta: any;
    globalEnvFilePath?: string | undefined;
}): Promise<void>;
